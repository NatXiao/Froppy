package ch.hevs.gdx2d

import ch.hevs.gdx2d.components.bitmaps.BitmapImage

class MovableObject(pos : Position) extends AnimatedObject(pos) {
  override var img: BitmapImage = ???
  var collider : pos.x + img.length
}
